<% content_for :title, "設定" %>

<div class="page-header">
  <h2>設定</h2>
</div>

<section class="card">
  <h3 class="card-title">飼料残量アラート</h3>

  <%= form_with model: @setting, url: setting_path, method: :patch do |form| %>
    <div class="form-group">
      <%= form.label :value, "アラートしきい値（kg）" %>
      <%= form.number_field :value, min: 1, class: "form-control" %>
      <p class="form-hint">残量がこの値以下になるとアラートを表示します（デフォルト: <%= Setting::DEFAULTS[Setting::FEED_STOCK_KEY] %>kg）</p>
      <% @setting.errors.where(:value).each do |error| %>
        <span class="field-error"><%= error.full_message %></span>
      <% end %>
    </div>

    <div class="actions">
      <%= form.submit "保存する", class: "btn btn-primary" %>
    </div>
  <% end %>
</section>

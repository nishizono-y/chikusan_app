<% content_for :title, "日次記録 詳細" %>

<h2 class="page-title">日次記録 詳細</h2>

<%= render "shared/detail_actions", back_path: daily_records_path(month: daily_record_month), edit_path: edit_daily_record_path(@daily_record), record: @daily_record %>

<div class="card">
  <%= render @daily_record %>
</div>

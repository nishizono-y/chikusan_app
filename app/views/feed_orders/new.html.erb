<% content_for :title, "発注記録 新規作成" %>

<div class="page-title-row">
  <h2 class="page-title">発注記録 新規作成</h2>
  <div class="page-title-actions">
    <%= link_to feed_orders_path, class: "btn btn-secondary btn-sm" do %>
      <i class="fa-solid fa-arrow-left"></i> キャンセル
    <% end %>
  </div>
</div>

<div class="card">
  <%= render "form", feed_order: @feed_order %>
</div>
